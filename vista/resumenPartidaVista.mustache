<!doctype html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Resumen de la Partida</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background: #f3f3f3;
        }

        .resumen-box {
            margin: 50px auto;
            padding: 30px;
            background: white;
            border-radius: 12px;
            max-width: 500px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.1);
            text-align: center;
        }

        .puntos {
            font-size: 48px;
            font-weight: bold;
            color: #0d6efd;
        }

    </style>
</head>

<body>

<h1 class="mt-5 text-center text-danger fw-bold">Incorrecta!</h1>

<div class="resumen-box">
    <h2>Resumen de la Partida</h2>

    <hr>

    <h4>Jugador:</h4>
    <p>{{usuario}}</p>

    <h4>Puntos obtenidos:</h4>
    <p class="puntos">{{puntos}}</p>


    <h4>Partida - id:</h4>
    <p>{{id}}</p>

    <h4>Partida - estado:</h4>
    <p>{{estado}}</p>

    <h4>Partida - inicio:</h4>
    <p>{{inicio}}</p>

    <h4>Partida - fin:</h4>
    <p>{{fin}}</p>

    <h4>Partida - tiempo(seg):</h4>
    <p>{{tiempo_total_seg}}</p>

    <hr>

    <a href="/home/mostrarHome" class="btn btn-secondary w-100 mt-3">Volver al menú</a>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
